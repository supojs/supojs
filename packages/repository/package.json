{
  "name": "@supo/repository",
  "version": "1.0.2",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build",
    "test": "npm run build"
  },
  "dependencies": {
    "@supojs/core": "^1.0.2"
  },
  "devDependencies": {
    "rimraf": "~3.0.2",
    "typescript": "~4.3.0"
  }
}
