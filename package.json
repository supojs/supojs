{
  "name": "supojs",
  "description": "SUPer Opinionated JS backend framework",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "lerna run clean",
    "build": "lerna run build",
    "publish": "lerna publish prerelease -y --force-publish",
    "test": "lerna run test",
    "lint": "eslint --ext js,ts ."
  },
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "@types/node": "^16.4.3",
    "await-to-js": "^3.0.0",
    "axios": "^0.21.1",
    "eslint": "~7.30.0",
    "lerna": "~4.0.0",
    "ts-jest": "^27.0.4",
    "typescript": "~4.3.0"
  },
  "engines": {
    "npm": ">=7"
  }
}
